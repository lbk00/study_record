# 2444 별찍기 문제 -> 이중 for문을 활용하여 해결 // for문의 첫번째 값과 조건식에 주의해야함
# 2675 문자열 반복 -> 반복문 안에서 특정 조건의 변수를 사용하려면 반복문이 끝날때 변수를 초기화 시켜줘야한다.
# 10798 세로읽기 -> 2차원 배열 초기화를 하지않으면 쓰레기 값 할당됨 // 0이나 '\0'로 초기화
# 2566 최댓값 -> 2차원 배열 최댓값 구하는 문제 // 배열을 scanf로 입력받을시 주소연산자 &를 붙여야함.
