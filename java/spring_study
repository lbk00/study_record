##자바공부 / 스프링부트
main 과 test 나누어져있음 실제 자바코드는 main안에
java파일을 제외한 파일은 resources(여러설정..)라고 봐도 무방
자바는 main메서드로부터 모든것이 시작됨
localhost:8080 연결해서 에러뜨면 성공적으로 환경설정된것
Dependencies -> 라이브러리 간의 의존관계
gradle은 의존관계가있는 라이브러리를 함께 다운로드함
스프링 부트는 Welcome Page 기능을 제공 -> static에 index.html추가 
